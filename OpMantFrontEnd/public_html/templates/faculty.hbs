<div class="modal-body">
    <table class="table table-hover table-bordered table-responsive">
        <tbody class="tbody-faculty">
            {{#each values}}
            <tr>
                <td>
                    <a href="#" field-value="{{id_faculty}}">{{name_faculty}}</a>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</div>